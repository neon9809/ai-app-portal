{
  "name": "ip-analyzer",
  "display_name": "IP 安全分析",
  "version": "1.0.2",
  "type": "python",
  "entry": "mod.py",
  "runtime": "persistent",
  "route": "ip-analyzer",
  "description": "批量分析 IP 安全状况：IPinfo 地理与 ASN 归属 + AbuseIPDB 恶意报告评分，风险分级，进度实时轮询，结果 CSV 导出。本包演示环境变量/机密注入与出站代理白名单。",
  "author": "Neon（移植自 neon9809/ip-analyzer）",
  "capabilities": ["db"],
  "network": ["api.abuseipdb.com", "ipinfo.io"],
  "requirements": ["flask>=3.0"],
  "env": {
    "ABUSEIPDB_API_KEY": {
      "required": true,
      "secret": true,
      "pattern": "^[a-f0-9]{80}$",
      "description": "AbuseIPDB API 密钥（abuseipdb.com/account/api 获取，80 位十六进制）"
    }
  }
}
