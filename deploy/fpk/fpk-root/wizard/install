[
  {
    "stepTitle": "服务设置",
    "items": [
      {
        "type": "tips",
        "helpText": "此端口为门户对外服务端口：内网通过 http://NAS地址:端口 直接访问，外网可用门户自带 HTTPS（ACME）或自有反代；桌面图标将打开该端口。默认 8080，如与现有服务冲突可修改。"
      },
      {
        "type": "text",
        "field": "wizard_port",
        "label": "服务端口",
        "initValue": "8080",
        "rules": [
          { "required": true, "message": "请填写端口" },
          { "pattern": "^[0-9]+$", "message": "仅数字" }
        ]
      },
      {
        "type": "tips",
        "helpText": "初始管理员密码可现在自设（推荐 12 位以上，建议含大小写/数字/符号），留空则系统随机生成并写入应用数据目录 admin-credentials.txt。无论哪种方式，首次登录都会强制设置新密码并绑定 MFA。"
      },
      {
        "type": "password",
        "field": "wizard_admin_password",
        "label": "初始管理员密码（留空则随机生成）",
        "rules": [
          { "pattern": "^$|^.{8,64}$", "message": "留空则随机生成；填写需 8-64 位" }
        ]
      }
    ]
  }
]
