{
  "name": "ai-app-portal",
  "version": "0.1.2",
  "license": "GPL-3.0",
  "private": true,
  "description": "AI应用门户 — 自托管 AI 应用网关 + 门户（统一入口 / 自动 HTTPS / 登录鉴权 / 主题品牌 / LLM 网关 / 计费）",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter @aap/shared build && pnpm -r --parallel dev",
    "build": "pnpm --filter @aap/shared build && pnpm -r build",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter @aap/shared build && pnpm --filter @aap/web build && playwright test -c e2e/playwright.config.ts",
    "typecheck": "pnpm -r typecheck",
    "db:generate": "pnpm --filter @aap/server db:generate"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "adm-zip": "^0.6.1",
    "otplib": "^13.5.0",
    "typescript": "^5.9.2"
  }
}
